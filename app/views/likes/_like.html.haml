.favorite-btn{:id => "like-button-#{addiction.id}"}
  - if current_user.likes.find_by(addiction_id: addiction.id)
    = link_to unlike_path(addiction.id), method: :delete, remote: true do
      .favorite-btn__star{:style => "background-color: red;"}
        %p ⭐️
  - else
    = link_to like_path(addiction.id), method: :post, remote: true do
      .favorite-btn__star
        %p ★
